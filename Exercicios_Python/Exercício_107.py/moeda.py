def aumentar(preco, taxa):
    valor = preco + (preco * taxa/100)
    return valor


def dobro(preco):
    valor = preco * 2
    return valor


def metade(preco):
    valor = preco / 2
    return valor
